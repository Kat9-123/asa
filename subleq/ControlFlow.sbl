Z Z .main
#Sublib
#Sublib/Control
**
@If s_conditional? body? {
    s_conditional? .true
    W W .false
    .true ->
    body?
    .false ->
}

@If::Leq a? b? body? {
    !If {!Leq a? b?} body?
}

@Leq a? b? .jp {
    a? -= b? .jp
}
**



x -> 0x8000
y -> 0x8000
W -> 0
Z -> 0

A -> 'A'
B -> 'B'

TEST -> 0
.main->{

    !IfElse::Eq x y {
        !IO::PrintLnLit "EQ"
    } {
        !IO::PrintLnLit "NEQ"
    }
    !BreakPoint
    !Halt



}
